FROM node:20-alpine

WORKDIR /app

# PDF 导出需要中文字体（用于在容器内正确渲染）
RUN apk add --no-cache font-noto-cjk

# 安装依赖
COPY package*.json ./
RUN npm ci

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 开发模式启动命令
CMD ["npm", "run", "dev"]



