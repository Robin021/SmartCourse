# MongoDB 配置
MONGODB_URI=mongodb://admin:admin123@localhost:27017/smartcourse?authSource=admin

# PostgreSQL 配置（向量数据库）
POSTGRES_URL=postgresql://postgres:postgres123@localhost:5432/smartcourse

# JWT 配置
JWT_SECRET=your-secret-key-change-in-production
NEXTAUTH_SECRET=your-nextauth-secret-change-in-production
NEXTAUTH_URL=http://localhost:3000
# Cookie 安全配置（生产环境保持为 true；HTTP 调试可设为 false）
# COOKIE_SECURE=true

# Next.js 环境
NODE_ENV=development

# 存储配置（可选）
# 如果使用本地存储，无需配置
# 如果使用 S3/MinIO/阿里云OSS，请在管理后台配置

# LLM 配置（可选）
# LLM 配置请在管理后台进行配置，无需在此设置



